apiVersion: v1
kind: Service
metadata:
  name: httpserver
  namespace: vote-demo
spec:
  type: NodePort
  selector:
    app: httpserver
  ports:
    - port: 8888
      targetPort: 8888
      nodePort: 30888
